!!!
%html
  %head
    %title RSS Reader
    = javascript_include_tag :defaults
    = stylesheet_link_tag 'application'
  %body
    #header
      - if current_user
        Logged in as #{current_user.email}
        = link_to "Log out", logout_user_path(current_user)
    = flash_messages
    = yield