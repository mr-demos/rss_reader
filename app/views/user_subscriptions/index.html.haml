%h2 Your Subscriptions
- if @subscriptions.empty?
  %p You have no subscriptions
- else
  %ul
    -@subscriptions.each do |subscription|
      %li
        = link_to subscription.title, user_subscription_path(@user, subscription)
%div
  = link_to "Add a subscription", new_user_subscription_path(@user)